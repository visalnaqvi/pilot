rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Browser SDK access is intentionally disabled. The application authorizes
    // short-lived upload and download URLs from authenticated server routes.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
